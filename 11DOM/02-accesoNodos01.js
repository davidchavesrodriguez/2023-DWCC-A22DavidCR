// A táboa con id="age-table".
const idTable= document.getElementById("age-table");

// Todos os elementos label dentro da táboa (debería haber 3).
const allTableElements= document.querySelectorAll("table label");

// O primeiro td da táboa.
const firstTableTd= document.querySelector("table td");

// O form con name="search".
const formName= document.getElementsByName("search");

// O primeiro input do formulario anterior.
const firstFormNameInput= document.querySelector("input");

// O último input do formulario do exercicio anterior.
const lastFormNameInput= document.querySelectorAll[-1]("input");