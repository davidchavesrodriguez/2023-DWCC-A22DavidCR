// Ejercicio 1: Longitud de la cadena

// Crea una función que tome una cadena como argumento y devuelva la longitud de la cadena.
let cadea = 'Chegarei a ser o mellor...';
function calcularLongitud(cadea) {
  console.log(cadea.length);
}
calcularLongitud(cadea);

// Ejercicio 2: Contar letras

// Escribe una función que tome una cadena y una letra como argumentos y devuelva el número de veces que aparece esa letra en la cadena.

// Ejercicio 3: Reemplazar espacios

// Crea una función que tome una cadena y reemplace todos los espacios en blanco por guiones bajos ('_').

// Ejercicio 4: Palíndromo

// Escribe una función que determine si una cadena es un palíndromo, es decir, si se lee igual de izquierda a derecha que de derecha a izquierda (ignorando espacios y signos de puntuación).

// Ejercicio 5: Mayúsculas y minúsculas

// Crea una función que tome una cadena como argumento y devuelva una nueva cadena en la que todas las letras estén en minúsculas, excepto la primera letra que debe estar en mayúscula.
