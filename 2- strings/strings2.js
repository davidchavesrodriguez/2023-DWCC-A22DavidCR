/* 2. Crea unha función á que se lle pase unha cadea e devolva a cadea en sentido
inverso. */

function reverseString(str) {
  console.log(`${str.split('').reverse().join('')}`);
}
reverseString('I am a string'); // gnirts a ma I
